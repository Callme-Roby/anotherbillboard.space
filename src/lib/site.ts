/**
 * The site's name — shared between the `<title>`/metadata (layout.tsx)
 * and the on-page header (components/scene/Header.tsx) so the two can't
 * silently drift apart.
 */
export const SITE_NAME = "Another Billboard";
